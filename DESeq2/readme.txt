The script supplied here is an example. It will process the results of RNA-Seq mapping part (obtained from docker) and compute the DEGs from 2 conditions (typically control versus treated). 
For actual use, the script should be adapted to the users setup (cf box of parameters to set manually).
